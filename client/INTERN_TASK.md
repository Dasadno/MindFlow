# Задание для Intern разработчика: Создание простой кнопки с иконкой

## 📋 Описание задачи

Создать простой UI-компонент **IconButton** — кнопку с иконкой для использования в интерфейсе чата.

## 📍 Где создавать

```
client/src/shared/ui/IconButton/
├── IconButton.tsx
└── IconButton.stories.tsx
```

## 🎨 Как должна выглядеть кнопка

Круглая кнопка с иконкой внутри, которая меняет цвет при наведении.

## 📝 Что нужно сделать

### Шаг 1: Создать папку для компонента

**Команда:**
```bash
cd /home/tima/Desktop/milk-island/client
mkdir -p src/shared/ui/IconButton
```

### Шаг 2: Создать файл компонента

**Путь:** `client/src/shared/ui/IconButton/IconButton.tsx`

**Скопируй этот код:**

```tsx
import { LucideIcon } from 'lucide-react';

interface IconButtonProps {
  icon: LucideIcon;
  onClick?: () => void;
}

export const IconButton = ({ icon: Icon, onClick }: IconButtonProps) => {
  return (
    <button
      onClick={onClick}
      className="
        p-2
        text-text-secondary
        hover:text-text-primary
        hover:bg-deep-midnight/50
        rounded-lg
        transition-colors
      "
    >
      <Icon className="w-5 h-5" />
    </button>
  );
};
```

### Шаг 3: Создать файл для Storybook

**Путь:** `client/src/shared/ui/IconButton/IconButton.stories.tsx`

**Скопируй этот код:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Settings, User, Bell, Search } from 'lucide-react';
import { IconButton } from './IconButton';

const meta: Meta<typeof IconButton> = {
  title: 'Shared/IconButton',
  component: IconButton,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<typeof IconButton>;

export const SettingsIcon: Story = {
  args: {
    icon: Settings,
  },
};

export const UserIcon: Story = {
  args: {
    icon: User,
  },
};

export const BellIcon: Story = {
  args: {
    icon: Bell,
  },
};

export const SearchIcon: Story = {
  args: {
    icon: Search,
  },
};

export const WithClick: Story = {
  args: {
    icon: Settings,
    onClick: () => alert('Button clicked!'),
  },
};
```

### Шаг 4: Создать файл экспорта

**Путь:** `client/src/shared/ui/IconButton/index.ts`

**Скопируй этот код:**

```tsx
export { IconButton } from './IconButton';
```

## 🚀 Как запустить и проверить

### 1. Запустить Storybook

**Команда:**
```bash
cd /home/tima/Desktop/milk-island/client
npm run storybook
```

### 2. Открыть в браузере

Storybook откроется автоматически или перейди на:
```
http://localhost:6006
```

### 3. Найти компонент

В левой панели Storybook найди:
```
Shared → IconButton
```

### 4. Протестировать

- Наведи мышку на кнопку — цвет должен измениться
- Нажми на кнопку в истории "WithClick" — должно появиться окно с сообщением

## ✅ Критерии приемки

Отметь галочками, когда выполнишь:

- [ ] Создана папка `client/src/shared/ui/IconButton/`
- [ ] Создан файл `IconButton.tsx`
- [ ] Создан файл `IconButton.stories.tsx`
- [ ] Создан файл `index.ts`
- [ ] Storybook запускается без ошибок
- [ ] Компонент отображается в Storybook
- [ ] Все 5 историй работают (Settings, User, Bell, Search, WithClick)
- [ ] При наведении цвет иконки меняется
- [ ] При клике на "WithClick" появляется alert

## 📚 Что означают эти слова?

- **Component (компонент)** — переиспользуемый кусок интерфейса (как кнопка, карточка)
- **Props (пропсы)** — параметры, которые передаются в компонент
- **Interface (интерфейс)** — описание типов данных в TypeScript
- **Storybook** — инструмент для разработки компонентов в изоляции
- **Lucide React** — библиотека иконок для React

## 💡 Подсказки

1. **Если Storybook не запускается:**
   ```bash
   npm install
   npm run storybook
   ```

2. **Если видишь ошибку импорта:**
   - Проверь, что все файлы созданы в правильных папках
   - Проверь, что имена файлов написаны правильно (с большой буквы)

3. **Если иконка не отображается:**
   - Убедись, что импортировал иконку из `lucide-react`
   - Проверь, что передал иконку в пропс `icon`

## 🎓 Что ты изучишь

- ✅ Создание React-компонента
- ✅ Работа с TypeScript интерфейсами
- ✅ Использование Tailwind CSS классов
- ✅ Работа с библиотекой иконок
- ✅ Создание историй в Storybook

## ⏱️ Примерное время выполнения

**20-30 минут**

## 🆘 Нужна помощь?

Если что-то не получается:
1. Перечитай инструкцию внимательно
2. Проверь, что все команды выполнены
3. Проверь, что код скопирован полностью
4. Обратись за помощью к наставнику

---

**Удачи! Ты справишься! 💪**
